*** Variables ***

${INPUT_EMAIL_LOGIN}      //div[@class='card__login']//input[@name='email']

${INPUT_SENHA_LOGIN}      //div[@class='card__login']//input[@name='password']

${BTN_ACESSAR}            text=Acessar

${AREA_LOGADA}            id=textName

${CONTA_LOGADA}           id=textAccountNumber

${SALDO_LOGADO}           id=textBalance

${BTN_LOGOUT}             id=btnExit
